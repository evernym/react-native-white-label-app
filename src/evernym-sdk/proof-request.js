// @flow

/*
 * Here is you can customize Proof Request dialog here.
 * */

// text which will be used as the header
export const HEADLINE = null

// text which will be used for top (accept) button.
export const ACCEPT_BUTTON_TEXT = null

// ext which will be used for bottom (deny) button.
export const DENY_BUTTON_TEXT = null

// custom component to use for Proof Request dialog rendering (instead of predefined one)
export const CustomProofRequestModal = null

// custom component to use for selecting a Credential for Proof requested attribute dialog rendering (instead of predefined one)
export const CustomSelectAttributeValueModal = null

// custom component to use for selecting a Credential for Proof requested group of attribute dialog rendering (instead of predefined one)
export const CustomSelectAttributesValuesModal = null

// custom component to use for entering a custom value Proof requested attribute dialog rendering (instead of predefined one)
export const CustomEnterAttributeValueModal = null
